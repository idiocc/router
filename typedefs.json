{
  "_idio.initRoutes": {
    "link": "https://github.com/idiocc/router#async-initroutesrouter-_goarouterconfig-routerconfig-watchconfig",
    "description": "The `init` function will scan files in the passed `dir` folder and add routes found for each method to the router. The default `dir` is `src/routes` and the config should be passed to control how the middleware and aliases are set up for each method."
  },
  "RoutesConfig": {
    "link": "https://github.com/idiocc/router#type-routesconfig",
    "description": "Options for the router."
  },
  "undefinedwatchRoutes": {
    "link": "https://github.com/idiocc/router#async-watchroutesconfig-watchconfig-watcher"
  },
  "_idio.RoutesConfig": {
    "link": "https://github.com/idiocc/router#type-routesconfig",
    "description": "Options for the router."
  },
  "_idio.watchRoutes": {
    "link": "https://github.com/idiocc/router#async-watchroutesconfig-watchconfig-watcher",
    "description": "After the routes were initialised, it is possible to pass the value returned by the `initRoutes` method to the `watchRoutes` function to enable hot-route reload on the development environment. Every change to the module source code will trigger an update of the route including its aliases. *The middleware and aliases changes are not currently implemented.*"
  },
  "_idio.chainRoute": {
    "link": "https://github.com/idiocc/router#chainrouteroute-middleware-arraystringmiddleware",
    "description": "Receives the route and returns an ordered array of middleware."
  }
}